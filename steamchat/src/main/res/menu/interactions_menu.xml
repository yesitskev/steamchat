<?xml version="1.0" encoding="utf-8"?>

<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:steam="http://schemas.android.com/apk/res-auto">

    <!-- Conversation filter -->
    <item
        android:id="@+id/action_search"
        android:title="@android:string/search_go"
        android:icon="@android:drawable/ic_menu_search"
        steam:showAsAction="ifRoom|collapseActionView"
        android:actionViewClass="android.widget.SearchView" />

    <!-- New chat actionbar icon -->
    <item
        android:title="Friends"
        android:id="@+id/action_friends"
        android:icon="@drawable/ic_action_social_chat"
        steam:showAsAction="always" />

    <!-- The following menu options will appear in the overflow -->
    <item
        android:title="New Chat"
        android:id="@+id/action_chat"
        steam:showAsAction="never" />

    <item
        android:title="Settings"
        android:id="@+id/action_settings"
        steam:showAsAction="never" />

    <item
        android:title="Status"
        android:id="@+id/action_status"
        steam:showAsAction="never" />

</menu>