<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">

    </style>

    <style name="Widget.SteamChat.ActionBar" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/steam_actionbar_primary</item>
        <item name="android:textColor">@color/steam_actionbar_text</item>
    </style>

    <!-- Custom Login text views -->
    <style name="Widget.SteamChat.EditTextFullScreen" parent="android:Widget.Holo.EditText">
        <item name="android:background">@drawable/login_edittext_background</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColorHint">@color/steam_login_text_hint</item>
        <item name="android:textColor">@color/steam_login_text_color</item>
        <item name="android:shadowColor">@color/steam_login_text_shadow</item>
    </style>-->

</resources>
